return {
    "saghen/blink.cmp",
    dependencies = { "rafamadriz/friendly-snippets" },
    version = "1.*",
    opts = { keymap = { preset = "enter" }, }, -- The "enter" present uses tab to select snippets and enter to accept.
}
